# 설치 안내 (윈도우 기준)

처음 한 번만 하면 됩니다. 다 합쳐서 30분 정도입니다.

순서대로만 따라오시면 됩니다. 중간에 막히면 그 단계 번호를 알려주세요.

---

## 1단계 · 파이썬 설치 (10분)

1. https://www.python.org/downloads/ 접속
2. 가운데 노란 버튼 **Download Python 3.x.x** 클릭
3. 받은 파일 실행
4. **⚠ 여기가 제일 중요합니다.** 설치 첫 화면 맨 아래에 있는
   **`Add python.exe to PATH`** 체크박스를 **반드시 켜세요.**
   이걸 놓치면 나중에 "python은 인식할 수 없는 명령"이라는 오류가 납니다.
5. `Install Now` 클릭 → 끝날 때까지 대기 → `Close`

### 확인

키보드에서 `윈도우키 + R` → `cmd` 입력 → 엔터. 검은 창이 뜹니다.
거기에 아래를 입력하고 엔터:

```
python --version
```

`Python 3.13.1` 처럼 버전이 나오면 성공입니다.
`인식할 수 없는 명령`이 나오면 4번의 체크박스를 놓친 겁니다. 파이썬을 지우고 다시 설치하세요.

---

## 2단계 · 코드 내려받기 (3분)

1. https://github.com/ejj1220b-design/project 접속
2. 왼쪽 위 브랜치 선택 버튼(기본값 `main`)을 눌러
   **`claude/ad-roas-filtering-automation-fhjjmb`** 선택
3. 오른쪽 초록색 **`Code`** 버튼 → **`Download ZIP`**
4. 받은 ZIP 파일을 **`C:\roasloop`** 에 압축 해제
   (다른 경로도 되지만, 한글이나 띄어쓰기가 없는 경로가 안전합니다)

압축을 풀면 폴더 안에 폴더가 하나 더 있을 수 있습니다.
`README.md`, `pyproject.toml`, `config` 폴더가 **바로 보이는** 위치가 맞습니다.

---

## 3단계 · 설치 명령 (2분)

1. 탐색기로 `C:\roasloop` 폴더를 엽니다
2. **주소창을 클릭**해서 경로를 지우고 `cmd` 입력 → 엔터
   (이러면 그 폴더 위치에서 검은 창이 열립니다)
3. 아래를 붙여넣고 엔터:

```
pip install -e .
```

한참 글자가 올라가다가 마지막에 `Successfully installed` 가 나오면 성공입니다.

### 확인

```
roasloop --help
```

명령어 목록이 나오면 설치 끝입니다.

---

## 4단계 · 토큰 넣기 (10분)

### 4-1. 필요한 값 4개

| 항목 | 어디서 |
|---|---|
| 액세스 토큰 | developers.facebook.com → 앱 → 도구 → 그래프 API 탐색기 |
| 광고 계정 ID | Ads Manager 주소창의 `act_` 로 시작하는 숫자 |
| 페이지 ID | 페이지 → 정보 → 페이지 ID |
| 픽셀 ID | 이벤트 관리자 → 데이터 소스 |

토큰 권한은 **`ads_read`** 만 있으면 성과 조회·판정·보고서까지 다 됩니다.
광고를 만드는 단계(`launch`)까지 갈 때 `ads_management` 를 추가하세요.

> 처음엔 `ads_read` 만 받으시길 권합니다. 권한이 없으면 실수로도 계정을 바꿀 수 없습니다.

### 4-2. 파일 만들기

`C:\roasloop` 폴더에 있는 **`.env.example`** 파일을 복사해서
이름을 **`.env`** 로 바꿉니다. (`.example` 부분을 지우면 됩니다)

`.env` 를 메모장으로 열어서 값을 채웁니다:

```
META_ACCESS_TOKEN=EAAB로시작하는긴문자열
META_AD_ACCOUNT_ID=act_1234567890
META_PAGE_ID=1234567890
META_PIXEL_ID=1234567890
ANTHROPIC_API_KEY=
```

`ANTHROPIC_API_KEY` 는 카피 생성(`breed`)에만 필요합니다. 지금은 비워두셔도 됩니다.

> ⚠ `.env` 파일은 절대 다른 사람에게 보내거나 깃허브에 올리지 마세요.
> 토큰은 계정 비밀번호나 마찬가지입니다. (자동으로 제외되게 설정해두긴 했습니다)

---

## 5단계 · 첫 실행

**여기서 하는 것들은 전부 읽기만 합니다. 계정에 아무 변화도 없습니다.**

### 5-1. 캠페인 목록 확인

```
roasloop campaigns
```

계정의 캠페인이 `대행사 / 미분류` 로 나뉘어 나옵니다.
`E_US` 로 시작하는 건 자동으로 대행사로 잡힙니다.

여기서 **지안님 캠페인 이름을 확인**하세요. 미분류 목록에 있을 겁니다.

### 5-2. 내 캠페인 등록

`C:\roasloop\config\ownership.yaml` 을 메모장으로 열어서,
`mine: []` 부분을 이렇게 바꿉니다:

```yaml
mine:
  - name: "5-1에서 확인한 내 캠페인 이름 그대로"
  - name: "두 개 이상이면 이렇게 줄을 추가"
```

이름을 **한 글자도 틀리지 않게** 복사해서 붙여넣으세요.

다시 `roasloop campaigns` 를 실행하면 `인하우스` 로 옮겨가 있어야 합니다.

### 5-3. 성과 가져오기

```
roasloop harvest --days 14
```

최근 14일 성과를 가져옵니다.

### 5-4. 판정 보기

```
roasloop judge
```

끌 것 / 살릴 것 / 예산 올릴 것이 나옵니다.

### 5-5. 보고서 만들기

```
roasloop report
```

`data\runs\날짜\report.md` 파일이 생깁니다. 메모장으로 열어서 내용을 복사해
대행사나 상무님께 보내시면 됩니다.

---

## 여기까지 하면

계정을 전혀 건드리지 않고 판정과 보고서를 받을 수 있습니다.
이 상태로 며칠 돌려보시고, 판정이 납득되면 그때 다음 단계로 가면 됩니다.

| 다음 단계 | 명령 | 필요한 것 |
|---|---|---|
| 승자 소재 속성 분석 | `roasloop dna` | 없음 |
| 새 카피·기획안 생성 | `roasloop breed` | Anthropic API 키 |
| 내 캠페인 광고 중단 | `roasloop apply --yes` | `ads_management` 권한 |
| 광고 대량 생성 | `roasloop launch` | `ads_management` 권한 |

---

## 자주 나는 오류

| 오류 메시지 | 원인과 해결 |
|---|---|
| `'python'은(는) ... 인식할 수 없습니다` | 1단계 4번 체크박스를 놓쳤습니다. 파이썬 재설치 |
| `'roasloop'은(는) ... 인식할 수 없습니다` | 3단계 `pip install -e .` 를 안 했거나 실패했습니다 |
| `환경변수가 비어 있습니다` | `.env` 파일 이름이나 위치가 잘못됐습니다. `C:\roasloop\.env` 여야 합니다 |
| `[400/190] Invalid OAuth access token` | 토큰이 만료됐습니다. 그래프 API 탐색기에서 새로 발급 |
| `[400/100] ... ads_read` | 토큰 권한이 모자랍니다. 권한을 추가해 다시 발급 |
| `설정 파일이 없습니다` | 검은 창이 `C:\roasloop` 위치가 아닙니다. 3단계 2번을 다시 |

막히면 **검은 창에 나온 글자를 그대로 복사해서** 알려주세요.
